import "./bootstrap";

// Define a function named 'hello' that returns the string "Hello".
function hello() {
    return "Hello";
}

/*
Note:
- This file is typically included in Laravel's frontend setup using Vite.
- It is automatically processed when running `npm run dev` (for development) or `npm run build` (for production).
- You can import and use this function anywhere in your JavaScript files.
*/
